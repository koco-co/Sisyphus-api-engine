# 循环控制

> 学习如何使用 For 和 While 循环执行重复任务。

---

## 📖 概述

循环控制支持 For 循环（固定次数）和 While 循环（条件循环），可用于批量操作、轮询检查等场景。

---

## ⚙️ YAML 关键字参考

| 关键字 | 类型 | 描述 |
|-------|------|------|
| type | loop | 步骤类型 |
| loop_type | string | for 或 while |
| loop_count | number | For 循环次数 |
| loop_condition | string | While 循环条件 |
| loop_variable | string | 循环变量名 |
| loop_max | number | 最大迭代次数 |
| loop_steps | array | 循环体步骤 |

---

## 💡 使用示例

### 示例1：For 循环

```yaml
- name: "批量创建用户"
  type: loop
  loop_type: for
  loop_count: 5
  loop_variable: "i"
  loop_steps:
    - name: "创建用户 ${i}"
      method: POST
      url: "${base_url}/users"
      body:
        username: "user_${i}"
```

### 示例2：While 循环

```yaml
- name: "轮询任务状态"
  type: loop
  loop_type: while
  loop_condition: "${task_status} != 'completed'"
  loop_max: 10
  loop_steps:
    - name: "检查状态"
      method: GET
      url: "${base_url}/tasks/${task_id}"
      extractors:
        - name: task_status
          path: "$.status"
    - type: wait
      seconds: 2
```

---

## 🔗 相关资源

- [上一章：重试与等待](./07_重试与等待.md)
- [下一章：并发执行](./09_并发执行.md)
