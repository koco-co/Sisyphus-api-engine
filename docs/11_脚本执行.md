# 脚本执行

> 学习如何在测试中执行 Python 脚本。

---

## 📖 概述

脚本执行允许在测试流程中运行 Python 代码，用于复杂数据处理、加密签名等场景。

---

## ⚙️ YAML 关键字参考

| 关键字 | 类型 | 描述 |
|-------|------|------|
| type | script | 步骤类型 |
| script | string | Python 脚本内容 |
| script_type | string | python |
| allow_imports | boolean | 是否允许导入模块 |

---

## 💡 使用示例

```yaml
- name: "执行脚本"
  type: script
  script: |
    import hashlib
    data = get_var("raw_data")
    signature = hashlib.md5(data.encode()).hexdigest()
    set_var("signature", signature)
  script_type: python
  allow_imports: true

- name: "使用脚本结果"
  method: POST
  url: "${base_url}/verify"
  headers:
    X-Signature: "${signature}"
```

---

## 🔗 相关资源

- [上一章：数据驱动](./10_数据驱动.md)
- [下一章：数据库操作](./12_数据库操作.md)
