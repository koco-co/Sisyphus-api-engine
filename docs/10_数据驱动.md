# 数据驱动

> 学习如何使用外部数据源驱动测试执行。

---

## 📖 概述

数据驱动测试允许从 CSV、JSON、数据库等外部数据源读取测试数据，实现用例参数化。

---

## ⚙️ YAML 关键字参考

| 关键字 | 类型 | 描述 |
|-------|------|------|
| data_source | object | 数据源配置 |
| data_source.type | string | csv/json/database |
| data_source.file_path | string | 数据文件路径 |
| data_iterations | boolean | 是否迭代数据 |
| variable_prefix | string | 变量前缀 |

---

## 💡 使用示例

### 示例1：CSV 数据驱动

```yaml
config:
  data_source:
    type: csv
    file_path: "data/users.csv"
  data_iterations: true
  variable_prefix: "data_"

steps:
  - name: "创建用户"
    method: POST
    url: "${base_url}/users"
    body:
      username: "${data_username}"
      email: "${data_email}"
```

### 示例2：JSON 数据驱动

```yaml
config:
  data_source:
    type: json
    file_path: "data/test_cases.json"
```

---

## 🔗 相关资源

- [上一章：并发执行](./09_并发执行.md)
- [下一章：脚本执行](./11_脚本执行.md)
