# 未实现功能任务列表

> **创建时间**: 2026-01-29
> **当前版本**: v1.0.0
> **整体完成度**: 97%
> **本文档状态**: 📋 待开发

---

## 📊 功能完成度概览

```
阶段一: 核心执行框架 ████████████████████ 100% ✅
阶段二: 增强功能       ████████████████████ 100% ✅
阶段三: 企业特性       ████████████████████ 100% ✅
阶段四: 测试与发布     █████████████████░░░  97% ⏳

总进度:  ███████████████████░  97%
```

**已完成**: 510个测试全部通过，核心功能完整实现
**未完成**: 4项核心增强功能 + 文档工具类任务

---

## 🎯 优先级说明

- **P0 - 高优先级**: 输入协议中承诺但未实现的核心功能
- **P1 - 中优先级**: 增强用户体验的功能
- **P2 - 低优先级**: 文档和工具类任务

---

## 📋 未实现功能清单

### 一、核心功能增强 (P0 - 高优先级)

#### 1. 复杂断言逻辑运算 ⏳

**状态**: ❌ 未实现
**优先级**: P0
**复杂度**: 中等
**预计工作量**: 4-6小时

**功能描述**:
支持 `and`/`or`/`not` 逻辑运算组合多个断言，实现复杂验证逻辑。

**参考文档**:
- `docs/API-Engine输入协议规范.md` 第 5.2 节

**预期用法**:
```yaml
validate:
  # 逻辑与 - 所有断言必须通过
  - and:
      - eq: ["status_code", 200]
      - eq: ["body.code", 0]
      - gt: ["body.data.count", 0]

  # 逻辑或 - 至少一个断言通过
  - or:
      - eq: ["body.data.status", "available"]
      - eq: ["body.data.status", "low_stock"]
      - eq: ["body.data.status", "out_of_stock"]

  # 逻辑非 - 断言必须不成立
  - not:
      - eq: ["body.error", "timeout"]

  # 嵌套组合
  - and:
      - or:
          - eq: ["body.data.type", "A"]
          - eq: ["body.data.type", "B"]
      - gt: ["body.data.value", 100]
      - not:
          - contains: ["body.data.flags", "deprecated"]
```

**实现要点**:
1. 扩展 `ValidationEngine` 支持逻辑运算符识别
2. 创建 `LogicalOperator` 枚举 (AND/OR/NOT)
3. 实现递归验证逻辑，处理嵌套结构
4. 更新 `ValidationRule` 模型，支持 `logical_operator` 和 `sub_validations` 字段
5. 更新 `V2YamlParser` 解析逻辑断言
6. 编写单元测试（覆盖所有组合场景）

**涉及文件**:
- `apirun/core/models.py` - 扩展 ValidationRule
- `apirun/validation/engine.py` - 实现逻辑运算
- `apirun/parser/v2_yaml_parser.py` - 解析复杂断言
- `tests/validation/test_logical_operators.py` - 新建测试文件

**验收标准**:
- [ ] 支持 and/or/not 三种逻辑运算
- [ ] 支持任意层级嵌套
- [ ] 错误提示清晰，能定位到具体哪个子断言失败
- [ ] 单元测试覆盖率 > 90%
- [ ] 集成测试通过

---

#### 2. 数据库查询模板函数 ⏳

**状态**: ❌ 未实现
**优先级**: P0
**复杂度**: 中等
**预计工作量**: 3-4小时

**功能描述**:
在变量渲染时支持执行数据库查询，将查询结果作为变量值。

**参考文档**:
- `docs/API-Engine输入协议规范.md` 第 4.3 节

**预期用法**:
```yaml
variables:
  # 单值查询
  product_price: "${db_query('SELECT price FROM products WHERE sku=%s', 'SKU123')}"
  user_name: "${db_query('SELECT name FROM users WHERE id=%s', 12345)}"

  # 可用于断言
  expected_price: "${db_query('SELECT price FROM products WHERE id=%s', '${product_id}')}"

validate:
  - eq: ["body.data.price", "${expected_price}"]
```

**实现要点**:
1. 在 `template_functions.py` 中添加 `db_query()` 函数
2. 函数签名: `db_query(sql: str, *params) -> Any`
3. 需要访问 `VariableManager` 中的数据库连接配置
4. 支持从 `config.variables` 中获取数据库连接别名
5. 处理查询结果：
   - 单行单列 → 返回标量值
   - 单行多列 → 返回字典
   - 多行 → 返回列表
6. 添加安全检查：只允许 SELECT 查询
7. 错误处理：连接失败、SQL错误、空结果等

**涉及文件**:
- `apirun/core/template_functions.py` - 添加 db_query 函数
- `apirun/core/variable_manager.py` - 暴露数据库连接访问
- `tests/core/test_template_functions.py` - 添加测试

**验收标准**:
- [ ] 支持标量查询（返回单值）
- [ ] 支持字典查询（返回行数据）
- [ ] 支持列表查询（返回多行）
- [ ] 只允许 SELECT 查询，拒绝 INSERT/UPDATE/DELETE
- [ ] 使用预编译语句防止 SQL 注入
- [ ] 单元测试通过

**安全性考虑**:
- ⚠️ 只允许查询操作，禁止修改数据的 SQL
- ⚠️ 使用预编译语句
- ⚠️ 设置查询超时（避免慢查询）
- ⚠️ 限制返回结果集大小（避免内存溢出）

---

#### 3. Wait 条件等待 (API/Database类型) ⏳

**状态**: ❌ 未实现
**优先级**: P0
**复杂度**: 中等
**预计工作量**: 4-5小时

**功能描述**:
扩展 `WaitExecutor`，支持基于 API 或 Database 查询结果的条件等待。

**参考文档**:
- `docs/API-Engine输入协议规范.md` 第 3.3 节

**预期用法**:
```yaml
teststeps:
  # API 条件等待
  - name: "等待异步任务完成"
    type: "wait"
    wait_condition:
      type: "api"
      request:
        method: "GET"
        url: "/jobs/status"
        headers:
          Authorization: "Bearer ${token}"
        params:
          job_id: "${job_id}"
      check: "body.data.status == 'completed'"
      timeout: 30      # 最大等待时间（秒）
      interval: 2      # 轮询间隔（秒）

  # Database 条件等待
  - name: "等待数据入库"
    type: "wait"
    wait_condition:
      type: "database"
      connection: "mysql_main"
      sql_type: "mysql"
      sql: "SELECT status FROM orders WHERE order_id = %s"
      params: ["${order_id}"]
      check: "rows[0].status == 'confirmed'"
      timeout: 10
      interval: 1
```

**实现要点**:
1. 扩展 `WaitExecutor`，添加 `wait_condition` 支持
2. 定义 `WaitCondition` 模型（type/request/check/timeout/interval）
3. 实现 API 条件等待：
   - 使用 APIExecutor 发起请求
   - 使用 ValidationEngine 验证 check 条件
   - 循环轮询直到条件满足或超时
4. 实现 Database 条件等待：
   - 使用 DatabaseExecutor 执行查询
   - 验证查询结果
   - 循环轮询
5. 更新 `V2YamlParser` 解析 wait_condition
6. 添加详细的日志输出（轮询进度）

**涉及文件**:
- `apirun/core/models.py` - 添加 WaitCondition 模型
- `apirun/executor/wait_executor.py` - 扩展条件等待逻辑
- `apirun/parser/v2_yaml_parser.py` - 解析 wait_condition
- `tests/executor/test_wait_executor.py` - 添加测试

**验收标准**:
- [ ] 支持 API 条件等待
- [ ] 支持 Database 条件等待
- [ ] 支持 Jinja2 模板渲染 check 条件
- [ ] 超时后抛出明确异常
- [ ] 轮询间隔可配置
- [ ] 单元测试和集成测试通过

---

#### 4. Mock 条件判断 ⏳

**状态**: ❌ 未实现
**优先级**: P0
**复杂度**: 中等
**预计工作量**: 3-4小时

**功能描述**:
扩展 Mock 服务器，支持基于请求内容的条件 Mock。

**参考文档**:
- `docs/API-Engine输入协议规范.md` 第 6.5 节

**预期用法**:
```yaml
teststeps:
  - name: "条件 Mock 示例"
    type: "api"
    request:
      method: "POST"
      url: "/payment/create"
      json:
        amount: 150

    # 方式一：基于请求体条件
    mock:
      enabled: true
      conditions:
        - if: "${request.body.amount > 100}"
          response:
            status_code: 200
            body:
              code: 1
              msg: "金额超限"
        - else:
          response:
            status_code: 200
            body:
              code: 0
              msg: "success"
              data:
                payment_id: "MOCK_123"

    # 方式二：基于请求路径/方法条件
    mock:
      enabled: true
      conditions:
        - match:
            method: "POST"
            url_pattern: "^/api/v1/.*"
          response:
            body:
              version: "v1"
        - match:
            method: "POST"
            url_pattern: "^/api/v2/.*"
          response:
            body:
              version: "v2"
```

**实现要点**:
1. 扩展 `MockRule` 和 `RequestMatcher` 模型
2. 添加 `condition` 字段（Jinja2 表达式）
3. 实现条件评估逻辑：
   - 解析 if 条件表达式
   - 评估 request body/headers/params
   - 匹配成功则返回对应 response
4. 支持多个条件的顺序匹配
5. 支持默认 else 条件
6. 更新 YAML 解析支持 mock.conditions

**涉及文件**:
- `apirun/mock/models.py` - 扩展 MockRule 模型
- `apirun/mock/server.py` - 实现条件匹配逻辑
- `apirun/parser/v2_yaml_parser.py` - 解析 mock.conditions
- `tests/mock/test_conditional_mock.py` - 新建测试文件

**验收标准**:
- [ ] 支持基于 request body 的条件判断
- [ ] 支持基于 request headers/params 的条件判断
- [ ] 支持多个条件按优先级匹配
- [ ] 支持 else 默认响应
- [ ] 条件表达式支持 Jinja2 语法
- [ ] 单元测试通过

---

### 二、文档和工具 (P1 - 中优先级)

#### 5. 迁移工具 (v0.x → v1.0) 📝

**状态**: ❌ 未实现
**优先级**: P1
**复杂度**: 中等
**预计工作量**: 4-5小时

**功能描述**:
提供旧版本 YAML 格式到 v1.0 格式的自动转换工具。

**实现要点**:
1. 创建 `apirun/migration/migration_tool.py`
2. 定义转换规则：
   - `teststeps` → `teststeps` (保持不变)
   - `request` → `request` (保持不变)
   - `validate` 格式调整
   - `extract` 格式调整
3. 实现 CLI 工具：`sisyphus-migrate <old_yaml>`
4. 支持批量转换：`sisyphus-migrate --dir ./tests/`
5. 生成转换报告

**涉及文件**:
- `apirun/migration/__init__.py` - 新建模块
- `apirun/migration/migration_tool.py` - 核心逻辑
- `apirun/migration/converter.py` - 转换规则
- `apirun/cli.py` - 添加 migrate 命令

**验收标准**:
- [ ] 能转换常见旧格式用例
- [ ] 转换后用例可执行
- [ ] 生成转换报告（成功/失败/警告）
- [ ] 支持批量转换
- [ ] 提供回滚功能（备份原文件）

---

#### 6. 性能优化和基准测试 🔧

**状态**: ⏳ 部分完成
**优先级**: P1
**复杂度**: 低
**预计工作量**: 2-3小时

**优化目标**:
```yaml
性能指标:
  单个API请求: < 500ms
  100个步骤用例: < 30s
  并发测试(10线程): 稳定运行
  内存占用: < 200MB
```

**优化方向**:
1. **变量渲染性能优化**:
   - 缓存已渲染的模板
   - 减少重复渲染
   - 优化 Jinja2 环境

2. **响应时间优化**:
   - 减少不必要的深拷贝
   - 优化 JSON 序列化
   - 异步I/O优化

3. **内存占用优化**:
   - 及时释放大对象
   - 优化变量快照存储
   - 流式处理大响应

**实现要点**:
1. 创建 `tests/performance/benchmark.py`
2. 建立性能基准测试
3. 使用 cProfile 分析性能瓶颈
4. 逐项优化并验证

**涉及文件**:
- `tests/performance/` - 新建性能测试目录
- `apirun/core/variable_manager.py` - 优化渲染逻辑
- `apirun/executor/step_executor.py` - 优化深拷贝

**验收标准**:
- [ ] 性能基准测试通过
- [ ] 达成性能指标目标
- [ ] 内存占用不泄漏
- [ ] 性能测试报告

---

### 三、文档编写 (P2 - 低优先级)

#### 7. API 参考文档 📚

**状态**: ❌ 未编写
**优先级**: P2
**预计工作量**: 6-8小时

**内容大纲**:
1. **核心模块 API**
   - V2YamlParser
   - VariableManager
   - TestCaseExecutor
   - ValidationEngine

2. **执行器 API**
   - APIExecutor
   - DatabaseExecutor
   - WaitExecutor
   - LoopExecutor
   - ConcurrentExecutor
   - ScriptExecutor

3. **提取器 API**
   - JSONPathExtractor
   - RegexExtractor
   - HeaderExtractor
   - CookieExtractor

4. **导出器 API**
   - JSONExporter
   - AllureExporter
   - JUnitExporter
   - HTMLExporter

**输出文件**: `docs/API_REFERENCE.md`

---

#### 8. 快速开始指南 📚

**状态**: ❌ 未编写
**优先级**: P2
**预计工作量**: 4-6小时

**内容大纲**:
1. **5分钟快速上手**
   - 安装
   - 第一个测试用例
   - 运行和查看结果

2. **核心概念**
   - 测试用例结构
   - 变量系统
   - 验证机制
   - 数据驱动

3. **常见场景示例**
   - HTTP API 测试
   - 数据库操作
   - 并发测试
   - Mock 使用

4. **最佳实践**
   - 用例设计原则
   - 数据管理
   - 错误处理

5. **故障排查**
   - 常见错误及解决方案
   - 调试技巧

**输出文件**: `docs/QUICK_START.md`

---

#### 9. 配置参考手册 📚

**状态**: ❌ 未编写
**优先级**: P2
**预计工作量**: 3-4小时

**内容大纲**:
1. **Config 配置详解**
2. **TestStep 配置详解**
3. **环境配置最佳实践**
4. **变量系统完整说明**
5. **验证规则完整列表**

**输出文件**: `docs/CONFIGURATION_REFERENCE.md`

---

#### 10. 打包发布准备 📦

**状态**: ⏳ 部分完成
**优先级**: P2
**预计工作量**: 2-3小时

**任务清单**:
- [ ] 完善 `pyproject.toml` 配置
- [ ] 版本号管理（使用 `bump2version`）
- [ ] 创建 `CHANGELOG.md`
- [ ] 编写发布说明（RELEASE_NOTES.md）
- [ ] PyPI 发布测试
- [ ] GitHub Release 创建

---

## 📅 开发计划建议

### Sprint 1: 核心功能增强 (1-2周)
优先实现 P0 级别的核心增强功能：

1. **Week 1**:
   - Day 1-2: 复杂断言逻辑运算
   - Day 3-4: 数据库查询模板函数
   - Day 5: 测试和修复

2. **Week 2**:
   - Day 1-2: Wait 条件等待
   - Day 3-4: Mock 条件判断
   - Day 5: 集成测试和文档更新

### Sprint 2: 工具和优化 (1周)
实现 P1 级别的工具类任务：

1. **迁移工具** (2-3天)
2. **性能优化** (2天)

### Sprint 3: 文档完善 (1-2周)
完成 P2 级别的文档编写：

1. **API 参考文档** (2天)
2. **快速开始指南** (1-2天)
3. **配置参考手册** (1天)
4. **打包发布** (1天)

---

## 🎯 里程碑

| 里程碑 | 目标 | 预计完成时间 |
|--------|------|-------------|
| M1: 核心功能增强 | 完成 4 项 P0 功能 | 2周 |
| M2: 工具和优化 | 完成迁移工具和性能优化 | 1周 |
| M3: 文档完善 | 完成所有文档 | 1-2周 |
| M4: v1.1 正式发布 | 打包发布到 PyPI | 里程碑完成后 |

---

## 📝 注意事项

### 开发原则
1. **测试优先**: 每个功能必须有对应的单元测试
2. **向后兼容**: 新功能不应破坏现有用例
3. **文档同步**: 代码和文档同步更新
4. **性能考量**: 关注性能影响，避免退化

### 代码规范
- 遵循 Google Python Style Guide
- 添加类型注解
- 编写详细的 Docstrings
- 单元测试覆盖率 > 85%

### Git 工作流
- 从 `main` 创建功能分支
- 提交前运行完整测试套件
- PR 需要通过 Code Review
- 使用语义化提交信息

---

**文档维护**: 每完成一个功能，及时更新本文档状态
**最后更新**: 2026-01-29
**维护者**: Sisyphus Testing Platform Team
