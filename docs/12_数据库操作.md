# 数据库操作

> 学习如何在测试中执行数据库查询和操作。

---

## 📖 概述

数据库操作步骤支持 MySQL、PostgreSQL、SQLite 等数据库，可执行查询、插入、更新等操作。

---

## ⚙️ YAML 关键字参考

| 关键字 | 类型 | 描述 |
|-------|------|------|
| type | database | 步骤类型 |
| database | string | 数据库连接名 |
| operation | string | query/execute/script |
| sql | string | SQL 语句 |

---

## 💡 使用示例

```yaml
- name: "查询用户数据"
  type: database
  database: "test_db"
  operation: query
  sql: "SELECT * FROM users WHERE id = ${user_id}"
  extractors:
    - name: db_username
      path: "$[0].username"

- name: "验证数据库数据"
  method: GET
  url: "${base_url}/users/${user_id}"
  validations:
    - type: eq
      path: "$.data.username"
      expect: "${db_username}"
```

---

## 🔗 相关资源

- [上一章：脚本执行](./11_脚本执行.md)
- [下一章：Mock服务器](./13_Mock服务器.md)
